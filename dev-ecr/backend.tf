terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 3.0"
    }
  }
}

provider "aws" {
  region  = "enter-your-aws-region"
  profile = "enter-your-aws-profile"
}
